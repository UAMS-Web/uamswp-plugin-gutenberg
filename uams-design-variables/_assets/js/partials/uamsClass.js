const uamsClassAdd = ( element, elementClass ) => {

    element.classList.add( elementClass );

}

const uamsClassRemove = ( element, elementClass ) => {

    element.classList.remove( elementClass );

}

const uamsClassToggle = ( element, elementClass ) => {


}

export { uamsClassAdd, uamsClassRemove, uamsClassToggle }